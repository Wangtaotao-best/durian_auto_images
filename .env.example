# Docker deployment environment variables example
# Copy to .env and fill in actual values

# Exposed port
DURIAN_PORT=8000

# OpenVINO CPU threads (recommend: physical core count, e.g. Xeon Silver 4309Y = 8 cores)
OMP_NUM_THREADS=8
